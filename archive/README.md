# Process Archive

This directory is the index for selected process drafts that were explicitly marked as worth preserving. Each archived artifact keeps its runnable source on an `archive/*` branch and an immutable Git tag.

Archive entries are created only after an explicit archival request. They are not candidates for routine branch cleanup.

## Cursor Tracker

- Branch: `archive/cursor-tracker`
- Tag: `archive/cursor-tracker-preview`
- Source commit: `8f016fb`
- Format: standalone interactive site
- Preserved behavior: the portrait follows the pointer through a 110-frame sprite and winks when selected
- Hero copy: “Move around. She follows.”

## Selected Work — Paper Drawer v1

- Branch: `archive/paper-drawer-v1`
- Tag: `archive/paper-drawer-v1`
- Source commit: `307ffe1`
- Format: runnable Portfolio snapshot
- Preserved behavior: a fixed-height project card with a paper drawer that rises over the project visual to reveal its TL;DR
